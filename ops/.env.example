# =============================================================================
# 9log ERP - Production Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - These must be set for the system to work
# -----------------------------------------------------------------------------

# Secret key for JWT tokens (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here

# Database connection (Docker internal)
DATABASE_URL=postgresql+psycopg://tms:tms@db:5432/tms

# Redis connection (Docker internal)
REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# STORAGE - MinIO/S3 Configuration
# -----------------------------------------------------------------------------
S3_ENDPOINT=http://minio:9000
S3_BUCKET=tms
S3_ACCESS=minio
S3_SECRET=miniostorage

# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------
# CORS origins (comma separated, or * for all)
CORS_ORIGINS=*

# Token expiration in minutes (default: 30 days)
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# -----------------------------------------------------------------------------
# AI PROVIDERS (Optional - can be configured via Admin UI)
# -----------------------------------------------------------------------------
# API keys can also be managed via /admin/ai-settings page
# These are fallback values if database config is empty

# Anthropic Claude API Key
ANTHROPIC_API_KEY=

# Google Gemini API Key
GEMINI_API_KEY=

# OpenAI API Key
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# NOTIFICATIONS (Optional)
# -----------------------------------------------------------------------------
# Telegram Bot for system notifications
# Create bot via @BotFather, get chat ID via @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_OWNER_CHAT_ID=

# -----------------------------------------------------------------------------
# EXTERNAL INTEGRATIONS (Optional)
# -----------------------------------------------------------------------------
# ECUS SQL Server connection for customs data sync
# ECUS_CONNECTION_STRING=DRIVER={ODBC Driver 17 for SQL Server};SERVER=...
